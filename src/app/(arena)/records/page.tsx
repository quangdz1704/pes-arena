import { Swords } from "lucide-react";
import { ComingSoon } from "@/components/shared/coming-soon";

export default function RecordsPage() {
  return <ComingSoon eyebrow="Thống kê" title="Kỷ lục" description="Những cột mốc đáng tự hào và cả lịch sử đau thương." phase={3} icon={Swords} />;
}
