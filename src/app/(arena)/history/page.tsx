import { History } from "lucide-react";
import { ComingSoon } from "@/components/shared/coming-soon";

export default function HistoryPage() {
  return <ComingSoon eyebrow="Thống kê" title="Lịch sử" description="Mọi trận đã lưu cùng bộ lọc người chơi, đội bóng và chế độ." phase={2} icon={History} />;
}
